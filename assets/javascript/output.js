// Make an ajax request.

//Then when the query loads, make a for statement that creates the divs and put the activity data within them.

for(i = 0; i < 6; i++){

    var newListItem = $("<div class = 'list-item' >");

    var 


}